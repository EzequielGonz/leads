# Frontend — URL del backend (sin /api). En desarrollo local no hace falta
# porque Vite proxea /api a localhost:5000.
# Ejemplo para Render:  VITE_API_URL=https://leads-backend.onrender.com
VITE_API_URL=

# Backend
PORT=5000

# Meta / WhatsApp (NO commitear estos valores reales)
META_ACCESS_TOKEN=
META_PHONE_NUMBER_ID=
META_BUSINESS_ACCOUNT_ID=
META_API_VERSION=v21.0
WEBHOOK_VERIFY_TOKEN=mi_token_secreto
